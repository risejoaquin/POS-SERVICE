<Window x:Class="PosCore.Views.ReasonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Motivo de Anulación" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Ingrese el motivo de la anulación" FontSize="18" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,20"/>
        
        <ComboBox x:Name="ReasonCombo" Grid.Row="1" FontSize="14" Height="35" Margin="0,0,0,10">
            <ComboBoxItem Content="Cliente se arrepintió" IsSelected="True"/>
            <ComboBoxItem Content="Error de cobro"/>
            <ComboBoxItem Content="Producto en mal estado / Merma"/>
            <ComboBoxItem Content="Otro"/>
        </ComboBox>
        
        <TextBox x:Name="OtherReasonBox" Grid.Row="2" FontSize="14" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,50" Visibility="Collapsed"/>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,0,0">
            <Button Content="Cancelar" Width="90" Height="35" Margin="0,0,10,0" Background="#E5E7EB" BorderThickness="0" Click="BtnCancel_Click"/>
            <Button Content="Confirmar" Width="100" Height="35" Background="#EF4444" Foreground="White" BorderThickness="0" Click="BtnConfirm_Click"/>
        </StackPanel>
    </Grid>
</Window>
