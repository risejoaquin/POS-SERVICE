<UserControl x:Class="PosCore.Views.Controls.ActionPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300"
             Background="White">
    <UserControl.Resources>
        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Acciones Principales -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <Button Content="Apertura/Corte Caja (F4)" Command="{Binding OpenShiftCommand}" ToolTip="Abrir o cerrar el turno actual, ingresando los montos correspondientes en caja." Background="#10B981" Foreground="White" Margin="0,0,0,10" Height="40" BorderThickness="0" FontWeight="Bold" FontSize="14">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
            </Button>
            <Button Content="Suspender Orden (F2)" Command="{Binding SuspendOrderCommand}" ToolTip="Pone la orden actual en espera para poder atender a otro cliente." Background="#F59E0B" Foreground="White" Margin="0,0,0,10" Height="40" BorderThickness="0" FontWeight="Bold" FontSize="14">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
            </Button>
            <Button Content="Recuperar Orden (F3)" Command="{Binding ResumeOrderCommand}" ToolTip="Recupera una orden previamente suspendida." Background="#3B82F6" Foreground="White" Height="40" BorderThickness="0" FontWeight="Bold" FontSize="14">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
            </Button>
        </StackPanel>

        <!-- Shortcuts 3x3 -->
        <GroupBox Header="Atajos Rápidos (Alt+1 a Alt+9)" Grid.Row="1" BorderBrush="#E5E7EB" Foreground="#374151" FontWeight="Bold" FontSize="14" Margin="0,0,0,15">
            <ItemsControl ItemsSource="{Binding Shortcuts}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3" Rows="3" Margin="5"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.ExecuteShortcutCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                CommandParameter="{Binding Action}" 
                                Background="{Binding Color}" 
                                Foreground="White" Margin="3" BorderThickness="0" Cursor="Hand" Height="75" ToolTip="{Binding Description}">
                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Icon}" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding Name}" FontSize="11" FontWeight="SemiBold" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </GroupBox>

        <!-- Espaciador -->
        <Border Grid.Row="2"/>

        <!-- Cobrar -->
        <Button Grid.Row="3" Command="{Binding CheckoutCommand}" ToolTip="Finaliza la orden actual, procesa el pago y emite el recibo." Background="#2D5F2E" Foreground="White" Height="80" BorderThickness="0" Cursor="Hand">
            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="COBRAR" FontWeight="Bold" FontSize="24" Margin="0,0,15,0" VerticalAlignment="Center"/>
                <TextBlock Text="[F12]" FontSize="16" Foreground="#9CA3AF" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
