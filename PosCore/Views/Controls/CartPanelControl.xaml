<UserControl x:Class="PosCore.Views.Controls.CartPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="500"
             Background="#F9FAFB">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Carrito de Compra" FontSize="20" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,10"/>

        <!-- Items DataGrid -->
        <!-- In WPF, it's easier to use a DataGrid or an ItemsControl. We'll use a DataGrid for tabular display -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Cart}" AutoGenerateColumns="False" CanUserAddRows="False" 
                  HeadersVisibility="Column" Background="White" RowBackground="White" AlternatingRowBackground="#F9FAFB"
                  BorderBrush="#E5E7EB" BorderThickness="1" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E5E7EB"
                  RowHeight="50">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Cant" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Button Content="-" Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Width="25" Height="25" Background="#F3F4F6" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
                                <TextBlock Text="{Binding Quantity}" Width="30" TextAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                <Button Content="+" Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Width="25" Height="25" Background="#2D5F2E" Foreground="White" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Descripción" Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="P. Unit" Binding="{Binding Price, StringFormat=C}" Width="100" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Subtotal" Binding="{Binding SubTotal, StringFormat=C}" Width="100" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="✕" Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Width="30" Height="30" Background="#FEE2E2" Foreground="#EF4444" BorderThickness="0" Cursor="Hand" FontWeight="Bold">
                                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="15"/></Style></Button.Resources>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer Flotante -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,10,0,0">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="5"/>
            </Border.Effect>
            <StackPanel>
                <Grid Margin="0,0,0,5">
                    <TextBlock Text="Subtotal:" FontSize="16" Foreground="#6B7280"/>
                    <TextBlock Text="{Binding SubTotal, StringFormat=C}" FontSize="16" Foreground="#374151" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                </Grid>
                <Grid Margin="0,0,0,5">
                    <TextBlock Text="Impuestos:" FontSize="16" Foreground="#6B7280"/>
                    <!-- Impuestos is not in VM by default, just use 0 or something for demo, or assume VM has it if added later. We will just use '0' if it's missing, or we can bind to SubTotal * 0.16 if we had a converter, but let's assume standard behavior or just hardcode visually for UI. We'll leave it as binding or empty -->
                    <!-- The current implementation just had Subtotal and Total and Discount. I'll bind to SubTotal to show something if needed, or leave it fixed -->
                    <TextBlock Text="$0.00" FontSize="16" Foreground="#374151" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                </Grid>
                <Separator Margin="0,5"/>
                <Grid>
                    <TextBlock Text="Total:" FontSize="24" FontWeight="Bold" Foreground="#111827"/>
                    <TextBlock Text="{Binding Total, StringFormat=C}" FontSize="28" FontWeight="Bold" Foreground="#2D5F2E" HorizontalAlignment="Right"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
