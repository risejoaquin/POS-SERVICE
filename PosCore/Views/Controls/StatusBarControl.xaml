<UserControl x:Class="PosCore.Views.Controls.StatusBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="800"
             Background="#2D5F2E">
        <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="15,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Column="0" Text="{Binding Settings.WhiteLabel.CompanyName, StringFormat='{}☕ {0}'}" FontSize="16" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
        
        <!-- Toast Notification Overlay inside StatusBar instead of popping up over everything, or we can keep it here if we want -->
        <TextBlock Grid.Column="1" Text="{Binding NotificationMessage}" Foreground="#FCD34D" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding IsNotificationVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        
        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
            <Button Content="⚙️" Command="{Binding OpenSettingsCommand}" Width="30" Height="30" Background="Transparent" Foreground="White" BorderThickness="0" Margin="0,0,15,0" ToolTip="Atajos y Configuración" Cursor="Hand" FontSize="16"/>
            <Button Content="🖨️" Command="{Binding ConfigurePrinterCommand}" Width="30" Height="30" Background="Transparent" Foreground="White" BorderThickness="0" Margin="0,0,15,0" ToolTip="Configurar Impresora" Cursor="Hand" FontSize="16"/>
            <Ellipse Width="12" Height="12" Fill="{Binding SyncStatusColor}" Margin="0,0,8,0" />
<Ellipse Width="12" Height="12" Fill="{Binding SyncStatusColor}" Margin="0,0,8,0" />
            <TextBlock Text="{Binding SyncStatusMessage}" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
