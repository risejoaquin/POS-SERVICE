<Window x:Class="PosCore.Views.ManagerOverrideWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Autorización de Gerente" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Se requiere autorización de gerente" FontSize="18" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,10"/>
        <TextBlock x:Name="ActionDescText" Text="Acción: ..." FontSize="14" Foreground="#4B5563" Margin="0,0,0,20" TextWrapping="Wrap"/>
        
        <TextBlock Text="PIN de Autorización:" FontSize="14" Foreground="#374151" Margin="0,0,0,5" Grid.Row="2"/>
        <PasswordBox x:Name="PinBox" Grid.Row="3" FontSize="20" Padding="5" Height="35" PasswordChar="*"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,20,0,0">
            <Button Content="Cancelar" Width="90" Height="35" Margin="0,0,10,0" Background="#E5E7EB" BorderThickness="0" Click="BtnCancel_Click"/>
            <Button Content="Autorizar" Width="100" Height="35" Background="#3B82F6" Foreground="White" BorderThickness="0" Click="BtnAuthorize_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
