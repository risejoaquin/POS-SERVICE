<Window x:Class="PosCore.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PosCore.Views"
        mc:Ignorable="d"
        Title="Iniciar Sesión" Height="400" Width="400" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Sistema POS" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30" Foreground="#2C3E50"/>
            
            <TextBlock Text="Usuario" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="35" Margin="0,0,0,15" Padding="5,0" FontSize="14" />
            
            <TextBlock Text="Contraseña" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Height="35" Margin="0,0,0,25" Padding="5,0" FontSize="14" />
            
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,15" HorizontalAlignment="Center" TextWrapping="Wrap"/>
            
            <Button Content="Ingresar" Command="{Binding LoginCommand}" Height="45" Background="#007BFF" Foreground="White" FontSize="16" FontWeight="Bold" BorderThickness="0" Cursor="Hand" />
            
            <ProgressBar IsIndeterminate="True" Height="4" Margin="0,15,0,0" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</Window>
