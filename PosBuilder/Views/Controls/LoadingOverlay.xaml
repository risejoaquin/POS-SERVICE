<UserControl x:Class="PosBuilder.Views.Controls.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Visibility="Collapsed" Panel.ZIndex="9999">
    <Grid Background="#B3000000">
        <Border Background="White" CornerRadius="8" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="15" ShadowDepth="0"/>
            </Border.Effect>
            <StackPanel>
                <ProgressBar x:Name="Spinner" IsIndeterminate="True" Width="60" Height="10" Margin="0,0,0,20"/>
                <TextBlock x:Name="MessageText" Text="Cargando..." FontSize="16" FontWeight="SemiBold" Foreground="#334155" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
