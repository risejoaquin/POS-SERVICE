<UserControl x:Class="PosBuilder.Views.Controls.FileBrowserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">
    <Grid>
        <Border x:Name="DropZone" Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="2" CornerRadius="8" 
                AllowDrop="True" Drop="DropZone_Drop" MouseLeftButtonDown="DropZone_MouseLeftButtonDown" Cursor="Hand" MinHeight="120">
            <Border.Style>
                <Style TargetType="Border">
                    
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F1F5F9"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <StackPanel x:Name="DefaultState" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False">
                    <TextBlock Text="📁" FontSize="30" HorizontalAlignment="Center"/>
                    <TextBlock Text="Arrastra una imagen o clic para explorar" Foreground="#64748B" FontSize="12" Margin="0,5,0,0"/>
                </StackPanel>
                
                <StackPanel x:Name="PreviewState" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False">
                    <Image x:Name="Thumbnail" Width="100" Height="100" Stretch="Uniform" Margin="0,0,0,10"/>
                    <TextBlock x:Name="FileNameText" Foreground="#334155" FontSize="12" TextWrapping="Wrap" MaxWidth="250" HorizontalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel x:Name="LoadingState" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False">
                    <ProgressBar x:Name="LoadingProgress" IsIndeterminate="True" Width="150" Height="4" Margin="0,0,0,10" Foreground="#3B82F6"/>
                    <TextBlock Text="Procesando archivo..." Foreground="#64748B" FontSize="12" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
