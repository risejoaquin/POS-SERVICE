<UserControl x:Class="PosBuilder.Views.Controls.ColorPickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="300">
    <StackPanel>
        <!-- Hex Input & Preview -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Border x:Name="ColorPreview" Width="40" Height="40" Background="#2D5F2E" CornerRadius="4" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,10,0"/>
            <StackPanel>
                <TextBlock Text="Color Hex" FontSize="12" Foreground="#64748B" Margin="0,0,0,2"/>
                <TextBox x:Name="HexInput" Width="100" Padding="5" FontSize="14" TextChanged="HexInput_TextChanged"/>
            </StackPanel>
        </StackPanel>
        
        <TextBlock Text="Paleta Predefinida" FontSize="12" Foreground="#64748B" Margin="0,0,0,5"/>
        <ItemsControl x:Name="PaletteItems">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="6" Rows="2"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Background="{Binding Brush}" Width="30" Height="30" Margin="2" Cursor="Hand" Click="PaletteColor_Click" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="15"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
